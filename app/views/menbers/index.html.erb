<div class = "video_new">
  <div class ="edit_zenntai">
    <h4>ユーザー一覧</h4>
    <% @menbers.each do |menber| %>

          <h5><%= link_to(menber.name, "/menbers/#{menber.id}") %></h5>

    <% end %>
  </div>
  </div>
